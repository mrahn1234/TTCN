<%= render "banner" %>
<div class="container">
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@category) do |f| %>

      <%= f.label :Category %>
      <%= f.text_field :name, class: 'form-control' %>
      <%= f.submit "Save changes", class: "btn btn-primary" %>
      <%= f.button "Cancel", class: "btn btn-danger" %>
    <% end %>
  </div>
</div> 	
</div>
